const initialState = {
  entities: {},
  flights: []
};

const reducer = (state = initialState, action = {}) => {
  let newState;

  switch (action.type) {

    default:
      newState = state
  }

  return newState;
}

export default reducer;